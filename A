SELECT
    f.full_folder_path AS folder_path,
    COALESCE(d.manual, 0) AS manual_count,
    COALESCE(d.automated, 0) AS automated_count
FROM Folders f
LEFT JOIN DirectCounts d
    ON d.folder_id = f.folder_id
ORDER BY f.full_folder_path
OPTION (MAXRECURSION 0);
