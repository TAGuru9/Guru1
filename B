echo ==========================================
echo DEBUG BASE64 PARAMETERS
echo ==========================================

echo TC_File filename:
echo %TC_File_FILENAME%

echo Data_File filename:
echo %Data_File_FILENAME%

echo PAYLOAD_FILE filename:
echo %PAYLOAD_FILE_FILENAME%

echo ------------------------------------------

powershell -NoProfile -Command ^
"if($env:TC_File){'TC_File RECEIVED'} else {'TC_File EMPTY'}"

powershell -NoProfile -Command ^
"if($env:Data_File){'Data_File RECEIVED'} else {'Data_File EMPTY'}"

powershell -NoProfile -Command ^
"if($env:PAYLOAD_FILE){'PAYLOAD_FILE RECEIVED'} else {'PAYLOAD_FILE EMPTY'}"

echo ==========================================
